; Reduce per-request filesystem stat() calls on slow Windows bind mounts.
; Files are re-checked at most every 2 seconds instead of on every request.
opcache.revalidate_freq=2
realpath_cache_size=4096K
realpath_cache_ttl=600
